<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_move_reversal_inherit_fe" model="ir.ui.view">
        <field name="name">account.move.reversal.form.inherit.fe</field>
        <field name="model">account.move.reversal</field>
        <field name="inherit_id" ref="account.view_account_move_reversal"/>
        <field name="arch" type="xml">
            <field name="journal_id" position="after">
                <field name="l10n_ao_fe_serie_id" options="{'no_create': True}" invisible="not move_ids"/>
            </field>
        </field>
    </record>
</odoo>
